/*Ordenamiento Shell */
#include <iostream>

using namespace std;

int main(){
    int numeros[] = {3,1,6,4,2,7};
    int aux, longitud;

    longitud = sizeof(numeros[0] / sizeof(numeros));

    return 0;
}